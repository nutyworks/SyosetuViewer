<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="novel"
            type="me.nutyworks.syosetuviewerv2.data.NovelEntity" />

        <variable
            name="viewModel"
            type="me.nutyworks.syosetuviewerv2.ui.novellist.NovelListViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:foreground="?android:attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:onClick="@{() -> viewModel.onClick(novel)}"
        android:padding="8dp">

        <TextView
            android:id="@+id/tv_novel_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{novel.title}"
            app:layout_constraintBottom_toTopOf="@id/tv_novel_writer"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_novel_writer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{novel.writer}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_novel_title" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>